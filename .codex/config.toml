# Project-scoped Codex defaults for PlayPulse.
# Keep user-specific model, sandbox, approval, credential, and provider settings
# in the user-level Codex config instead of this repository.

project_doc_max_bytes = 65536

[agents]
max_threads = 6
max_depth = 1
